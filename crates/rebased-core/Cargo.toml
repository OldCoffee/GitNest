[package]
name = "rebased-core"
version.workspace = true
edition.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
similar = "2"
thiserror = "2"
base64 = "0.22"

[dev-dependencies]
tempfile = "3"
